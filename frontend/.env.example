# No frontend environment variables are currently required or consumed.
# Browser API requests must use relative URLs, for example /api/health.
# VITE_* variables are public build-time values embedded in browser assets.
# Never use them for secrets. Runtime Compose environment cannot change them.
# Docker excludes .env files and currently defines no frontend build arguments.
# If a public setting is added later, wire that specific setting into the build.
